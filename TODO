Backlog:

Reeadme.md

DoxyGen

CI/CD

Отладочный файл, выбор из argv

Комментарии в файле для теста

Именованные значения в файлах для теста %s = %lg

10. Добавить enum в switch (согласовать)

13. передача структур по указателю (согласовать с Дедом и Соней)

15. malloc/calloc

19. Подрубить CI/CD из Github (регаться под фт акком)

20. Разобраться со stopImmediately,как избежать копипастинга и завершать родительскую функцию?Дед

22.Из п.21 сделать функцию для инициализации переменных род. функции через calloc

25.Добавить ONDEBUG в отладочный вывод

26.Вынести IO в отдельные файлы

27.Усилить тесты

In progress:



In review:


Cancelled:
ON_DEBUG в QuadraticSolver.cpp, CmpDouble.cpp (усложняет архитектуру и ничего не даёт взамен,
 т.к. в обоих файлах мало отладочного вывода)

12. структура для хранения коэфов, числа коренй и корней (не нужно по словам Деда)


Done:
Чтобы сетка рисовалась через ORIGIN


Устранить магические числа в граф режиме


Внедрить библиотеку в основной проект.


Применить ф-ии ввода коэфов в граф режиме

УПорядочить1 функции

Добавитьт escape последовательности для смены цвета вывода в консоль

Обработать inf, nan и прочие непотребства, чтобы программа закцикливалась

Подавить warning const char * const 
24.Показать дедуwarning with const char * const


ДОБАВИТЬ ГРАФ ВЫВОДRAYLIB
Избавиться от копипасты при разных форматах ввода (вводить через fscanf())

18. Добавить файловый ввод вывод для тестирования

Добавить unit тесты
17. Добавить юнит тестирование. Написать функцию runSingleTest(), runAllTests()

23.Убрать копипаст из getResumeLine-getModeLine (создать структуру с массивом строк,
 находить макс длину с помощью strlen(sprintf()) и 9, чтобы не лутать warning )

Классное меню!

Разберись с  (getresumeLine) RESUME_LINE:Got an unknown error during resume() function!

11. Вынести switch в функции.А зачем мы статусы в строки преобразуем, если можем сразу строки возвращать?
Кстати,resume()оставил в покое, т.к. она играет важную роль в другой функции, а возврат строки испрортит.


1. enum для статусов ошибок и количества решений +
2. struct для хранения коэфов +
3. Локализация ошибочного коэфа при вводе +
4. Отдельная функция для ввода +
5. Втрой вариант инициали0ации структуры с указателями +
6. Функция ввода с повтором ввода +
7. П.4. с вводом попыток? +
8. Выводить сколько осталось попыток +
9. no/yes для ввода +
14. QuadraticSolver.cpp исправить return +
16. Починить getPositiveIntInputSafe + (УРА!)
